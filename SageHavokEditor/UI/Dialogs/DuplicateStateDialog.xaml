<Window x:Class="SageHavokEditor.UI.Dialogs.DuplicateStateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Duplicate State" Width="460" MinHeight="250"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDarkBrush}"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText" Grid.Row="0" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TextBlock Grid.Row="1" Margin="0,0,0,4" Text="Name for the copy:"/>
        <TextBox x:Name="TxtName" Grid.Row="2" Padding="5" Margin="0,0,0,12"/>

        <CheckBox x:Name="ChkGenerator" Grid.Row="3" Margin="0,0,0,8" IsChecked="True"
                  Click="Option_Changed"
                  ToolTip="Unchecked, the copy points at the same generator as the original — editing that generator then changes both states."/>

        <CheckBox x:Name="ChkTransitions" Grid.Row="4" Margin="0,0,0,10" IsChecked="True"
                  Click="Option_Changed"
                  ToolTip="Unchecked, the copy starts with no outgoing transitions."/>

        <TextBlock x:Name="SummaryText" Grid.Row="5" Margin="0,0,0,12" TextWrapping="Wrap"
                   FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Duplicate" Width="90" Margin="0,0,8,0" Click="BtnOk_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="70" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
