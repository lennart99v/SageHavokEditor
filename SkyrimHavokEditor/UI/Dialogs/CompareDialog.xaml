<Window x:Class="SkyrimHavokEditor.UI.Dialogs.CompareDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SkyrimHavokEditor"
        Title="Compare Behavior Files"
        Height="650" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDarkBrush}"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- File pickers -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- File A -->
            <Border Grid.Column="0" Background="{StaticResource BgPanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="4" Padding="10,8">
                <StackPanel>
                    <TextBlock Text="File A (current)" FontWeight="Bold" 
                               Foreground="{StaticResource AccentBlueBrush}" Margin="0,0,0,4"/>
                    <TextBlock x:Name="FileALabel" Text="(loaded file)"
                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>

            <TextBlock Grid.Column="1" Text="⇄" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"/>

            <!-- File B -->
            <Border Grid.Column="2" Background="{StaticResource BgPanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="4" Padding="10,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="File B" FontWeight="Bold"
                               Foreground="{StaticResource AccentGreenBrush}" Margin="0,0,0,4"/>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="FileBLabel" Text="No file selected"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Browse..." Padding="8,3"
                                Margin="8,0,0,0" Click="BtnBrowseB_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Filter options -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,8">
            <CheckBox x:Name="ChkVariables"  Content="Variables"  IsChecked="True" Margin="0,0,12,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="ChkEvents"     Content="Events"     IsChecked="True" Margin="0,0,12,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="ChkClips"      Content="Clips"      IsChecked="True" Margin="0,0,12,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="ChkObjects"    Content="Objects"    IsChecked="True" Margin="0,0,20,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="ChkHideSame"   Content="Hide identical" IsChecked="True" Margin="0,0,12,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <TextBlock x:Name="SummaryText"  Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
        </WrapPanel>

        <!-- Column headers -->
        <Grid Grid.Row="2" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Type"     FontWeight="Bold" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="Diff"     FontWeight="Bold" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="2" Text="Name/ID"  FontWeight="Bold" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="3" Text="File A"   FontWeight="Bold" FontSize="11" Foreground="{StaticResource AccentBlueBrush}"/>
            <TextBlock Grid.Column="4" Text="File B"   FontWeight="Bold" FontSize="11" Foreground="{StaticResource AccentGreenBrush}"/>
        </Grid>

        <!-- Results -->
        <ListBox x:Name="ResultsList" Grid.Row="3"
                 SelectionChanged="ResultsList_SelectionChanged"
                 VirtualizingPanel.IsVirtualizing="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Category -->
                        <TextBlock Grid.Column="0" Text="{Binding Category}"
                                   FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"/>

                        <!-- Diff type badge -->
                        <Border Grid.Column="1" CornerRadius="3" Padding="5,2"
                                HorizontalAlignment="Left" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#2A3A2A"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DiffType}" Value="Added">
                                            <Setter Property="Background" Value="#1A3A1A"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding DiffType}" Value="Removed">
                                            <Setter Property="Background" Value="#3A1A1A"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding DiffType}" Value="Modified">
                                            <Setter Property="Background" Value="#3A2A00"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding DiffType}" FontSize="10" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DiffType}" Value="Added">
                                                <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding DiffType}" Value="Removed">
                                                <Setter Property="Foreground" Value="#FF6B6B"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding DiffType}" Value="Modified">
                                                <Setter Property="Foreground" Value="Orange"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <!-- Name -->
                        <TextBlock Grid.Column="2" Text="{Binding Name}"
                                   FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" Margin="4,0"/>

                        <!-- Value A -->
                        <TextBlock Grid.Column="3" Text="{Binding ValueA}"
                                   FontSize="11" Foreground="{StaticResource AccentBlueBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" Margin="4,0"/>

                        <!-- Value B -->
                        <TextBlock Grid.Column="4" Text="{Binding ValueB}"
                                   FontSize="11" Foreground="{StaticResource AccentGreenBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" Margin="4,0"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Footer -->
        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Export Diff" Click="BtnExportDiff_Click"
                    Padding="12,5" Margin="0,0,5,0"/>
            <Button Content="Close" Click="BtnClose_Click" Padding="12,5"/>
        </StackPanel>
    </Grid>
</Window>